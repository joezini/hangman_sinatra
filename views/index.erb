<html>
<head>
	<link rel="stylesheet" type="text/css" href="hangman-style.css">
</head>
<body>
	<h1>Hangman</h1>
	<img src="<%= img_src %>">
	<% if game_over %>
	<p>Game over!</p>
	<% end %>
	<p><%= word_so_far %></p>
	<p>Guessed so far: <%= guessed_so_far %></p>
	<form>
		<input type="text" name="guess" maxlength="1" size="1">
		<input type="submit" value="Guess!">
	</form>
</body>